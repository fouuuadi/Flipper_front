# Assets 3D — commités en brut dans git
#
# Git LFS a été abandonné : il nécessitait que CHAQUE machine (y compris la
# borne de déploiement) ait fait `git lfs install` AVANT le clone, sinon seul le
# pointeur (~130 octets) était récupéré et le modèle 3D ne se chargeait jamais.
# La borne du prof n'ayant pas LFS configuré, le binaire n'était jamais mis à
# jour côté machine. On stocke donc les `.glb` directement comme blobs git.
